services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./app:/app
    command: make dev
    environment:
      NODE_ENV: development

  caddy:
    image: caddy:2.6
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./services/caddy/config/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app

volumes:
  caddy_data:
  caddy_config: